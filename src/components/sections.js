import React from 'react';
import Section from './section';

export default () => (
  <section>
    <Section id='todo' name='To-do' />
    <Section id='doing' name='Doing' />
    <Section id='done' name='Done' />
  </section>
);
